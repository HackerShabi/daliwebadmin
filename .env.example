# Admin Panel Environment Variables
# Point to the frontend API routes where authentication data is stored
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXT_PUBLIC_ADMIN_API_URL=http://localhost:3000

# Admin Panel Password Protection
# Set this password to protect access to the admin panel
# Default fallback password is 'shoaib@255' if not set
NEXT_PUBLIC_ADMIN_PASSWORD=shoaib@255

# For production, point to the deployed frontend domain
# NEXT_PUBLIC_API_URL=https://your-frontend-domain.vercel.app
# NEXT_PUBLIC_ADMIN_API_URL=https://your-frontend-domain.vercel.app

# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster0.xxxxx.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0

# NextAuth Configuration
NEXTAUTH_URL=https://your-admin-domain.vercel.app
NEXTAUTH_SECRET=your-nextauth-secret-key

# Google OAuth Configuration (Required for NextAuth)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret

# IMPORTANT: For Vercel deployment, you MUST add these environment variables in your Vercel dashboard:
# 1. Go to your Vercel project settings
# 2. Navigate to Environment Variables section
# 3. Add each variable above with its corresponding value
# 4. Set the correct environment scope (Production, Preview, Development)
# 5. Update NEXT_PUBLIC_API_URL to point to your deployed frontend domain
# 6. Update NEXTAUTH_URL to point to your deployed admin domain
# 7. **CRITICAL**: Set NEXT_PUBLIC_ADMIN_PASSWORD to 'shoaib@255' for admin panel access

# Security Note:
# - Never commit the actual .env.local file to the repository
# - The ADMIN_PASSWORD should be stored securely in Vercel environment variables
# - Use strong passwords in production environments